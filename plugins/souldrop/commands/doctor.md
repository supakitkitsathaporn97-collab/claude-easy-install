---
description: Health check — verifies your assistant's install, fixes what it can, and shows a friendly report / Kiểm tra sức khỏe trợ lý — tự sửa lỗi nếu có
---

Run the `doctor` skill from this plugin now, start to finish: check every
part of the SoulDrop install (engine, plugin, profile, memory, second brain,
extras), quietly auto-fix what can be fixed, then show the user ONE friendly
bilingual health card — plain words, zero jargon, never a raw error.
