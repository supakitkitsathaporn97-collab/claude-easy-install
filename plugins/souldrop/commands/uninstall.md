---
description: Cleanly removes SoulDrop and its extras — your profile, memories and notes always stay / Gỡ SoulDrop sạch sẽ — hồ sơ, ký ức và ghi chú của bạn luôn được giữ lại
---

Run the `uninstall` skill from this plugin now, start to finish: confirm once
in plain words, remove the SoulDrop plugin, marketplace and the optional
extras this installer added (read `~/.souldrop/installed.json` to know which),
and end by telling the user exactly what was kept — their brain files are
NEVER deleted.
